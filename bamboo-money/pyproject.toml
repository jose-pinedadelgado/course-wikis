[project]
name = "bamboo-money-wiki"
version = "0.1.0"
description = "Product wiki for Bamboo Money — privacy-first personal finance app"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["docs"]

[tool.uv]
dev-dependencies = [
    "mkdocs-material>=9.6",
]
